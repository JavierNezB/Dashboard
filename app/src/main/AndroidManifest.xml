<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="javiermarcos.jc.dashboard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/vision72"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/vision72"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProdVendZonaActivity"></activity>
        <activity android:name=".IndiceProductosActivity" />
        <activity android:name=".ProductosActivity" />
        <activity android:name=".VentaZonasActivity" />
        <activity android:name=".ZonasActivity" />
        <activity android:name=".ventasAxoActivity" />
        <activity android:name=".VentasMenActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>